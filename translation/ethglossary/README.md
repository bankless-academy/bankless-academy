# ETHGlossary snapshot (vendored)

Source: https://ethglossary.visual-20-hoists.workers.dev (API v1), MPL-2.0.
Snapshot date: 2026-08-08 (532 terms, 24 languages — see `info.json`).

Canonical Ethereum terminology translations with per-context variants
(prose/heading/tag/ui/code) plus the English style guide. Reference data for
`translate-content`: pin glossary keyword translations and enforce consistent
terminology inside lesson translation prompts.

Not consumed by the app at runtime. Refresh by re-running the fetch against
`/api/v1/translations/<lang>` per language (or check if the service still
exists; this snapshot is the fallback if it doesn't).
